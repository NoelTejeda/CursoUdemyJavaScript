/**
 * nodejs: permite la ejecución de javascript fuera de los navegadores, especificamente en los servidores 
 * 
 * npm: node package manager, es el gestor de paquetes de node, ayuda a gestionar los paquetes que usemos en nuestro proyecto así como generar proyectos con esta herramienta, también puede ejecutar otra tareas más avanzadas como la ejecución de script, etc.
 * 
 * npm al generar un archivo va a generarnos un package.json esto es una declaración pública de las intenciones que tiene nuestro programa; es decir; en este archivo se van a encontrar todos los datos necesarios acerca de nuestro software como: las dependencias que usa, la versión, si tiene repositorio en github, autor.
 * 
 * el package.json esta escrito en json.  
 * 
 * json = javascript object notetion, es decir que json va a tener la escritura de un objeto javascript, de allí su nombre.lo que quiere decir que va a estar estructurado por un objeto con clave=valor
 * 
 * para que las dependencias existan en nuestro proyecto van a estar dentro de una carpeta llamada "node moduls" esta carpeta va a tener el paquete 
 * 
 * 
 * 
 * "TIPOS DE RESPUESTAS HTTP"
 * del servidor al cliente de
 * 
 * 100 de tipo informativo, por ejemplo un estado 102 significa que algo se está procesando en nuestro servidor.
 * 200 indican respuestas de éxito
 * 300 indican respuestas de redirección
 * 400 indican respuestas de errores en el cliente
 * 500 indican repuestas de errores en el servidor.
 */